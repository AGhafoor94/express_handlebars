{{> modal}}
<section class="hero is-medium is-dark is-bold">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                Burgers
            </h1>
            <button id="displayModal" class="button is-primary is-large">Add Burger</button>
        </div>
    </div>
</section>
<div class="columns">
    <ul class="column is-half">
        <p class="subtitle headers">Eat Burger</p>
        {{#each burgers}}
        {{> burgers-notEaten}}
        {{/each}}
    </ul>
    <ul class="column is-half">
        <p class="subtitle headers">Burgers Eaten</p>
        {{#each burgers}}
        {{> burgers-eaten}}
        {{/each}}
    </ul>
</div>