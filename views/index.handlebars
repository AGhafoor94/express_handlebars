{{> modal}}
<section class="hero is-medium is-dark is-bold">
    <div class="hero-body">
        <div class="container">
            <h1 class="title">
                Burgers
            </h1>
            <h2 class="subtitle">
                <button id="displayModal" class="button is-warning">Add Burger</button>
            </h2>
        </div>
    </div>
</section>
{{#each burgers}}
{{> burgers-block }}
{{/each}}