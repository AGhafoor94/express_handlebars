{{#unless eaten}}
<li>
    {{burger_name}}
    <button class="button is-danger eatBtn" data-id="{{id}}" data-eaten="{{eaten}}">Eat</button>
</li>
{{#if eaten}}
<li>
    {{burger_name}}
</li>
{{/if}}
{{/unless}}